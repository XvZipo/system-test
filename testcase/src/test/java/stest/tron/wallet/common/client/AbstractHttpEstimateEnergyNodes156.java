package stest.tron.wallet.common.client;

/** HTTP endpoint URLs used by EstimateEnergy HTTP tests (indices 1, 5, 6). */
public abstract class AbstractHttpEstimateEnergyNodes156 {

  protected String httpnode = HttpNodeList.get(1);
  protected String httpSoliditynode = HttpNodeList.get(5);
  protected String httpPbftNode = HttpNodeList.get(6);
}

