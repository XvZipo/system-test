package stest.tron.wallet.common.client;

/** HTTP endpoint URL from {@code httpnode.ip.list} index 0. */
public abstract class AbstractHttpNode0 {

  protected String httpnode = HttpNodeList.get(0);
}

