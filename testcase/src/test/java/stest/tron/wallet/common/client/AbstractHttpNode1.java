package stest.tron.wallet.common.client;

/** HTTP endpoint URL from {@code httpnode.ip.list} index 1. */
public abstract class AbstractHttpNode1 {

  protected String httpnode = HttpNodeList.get(1);
}

