package stest.tron.wallet.common.client;

/** HTTP endpoint URLs from {@code httpnode.ip.list} indices 0 and 1. */
public abstract class AbstractHttpNodes01 {

  protected String httpnode = HttpNodeList.get(0);
  protected String httpnode1 = HttpNodeList.get(1);
}

